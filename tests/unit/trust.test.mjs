import test from"node:test";import assert from"node:assert/strict";import{accessibilityMatch,trustState}from"../../src/trust.js";const item={suppressed:false,lastVerified:"2026-08-18",trust:{evidenceGrade:"A",liveToday:false,hoyVerified:false,ownerConfirmed:false},accessibility:{status:"Not publicly verified"}};test("fresh grade A research remains research, not owner/live truth",()=>assert.equal(trustState(item,new Date("2026-08-19T00:00:00Z")).key,"RESEARCH_A"));test("live today outranks research",()=>assert.equal(trustState({...item,trust:{...item.trust,liveToday:true}},new Date("2026-08-19T00:00:00Z")).key,"LIVE_TODAY"));test("unknown accessibility cannot satisfy a requested positive requirement",()=>assert.deepEqual(accessibilityMatch(item,true),{passes:false,positive:false}));test("explicit verified accessibility can satisfy a requested positive requirement",()=>assert.deepEqual(accessibilityMatch({...item,accessibility:{status:"Verified accessible"}},true),{passes:true,positive:true}));test("explicit negative access evidence is not a positive accessibility match",()=>assert.deepEqual(accessibilityMatch({...item,accessibility:{status:"No step-free access indicated"}},true),{passes:false,positive:false}));test("partial or improvement-only access evidence does not become a positive fit",()=>{assert.equal(accessibilityMatch({...item,accessibility:{status:"Partly verified – access infrastructure improved 2026"}},true).passes,false);assert.equal(accessibilityMatch({...item,accessibility:{status:"Natural terrain / partly supported access"}},true).passes,false)});
